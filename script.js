const gameBoard = (() => {
    const startGame = () => {
        let arrayBoard = [];
    };
    const findWinner = (arrayBoard) =>{

    };
    return {startGame, findWinner};
})();

const displayController = (() =>{
    const addMark = (player) => {

    };
    const displayWinner = () => {}
    return {addMark, displayWinner}
})();

const createPlayer = (player) => {
    let name = player;
    const chooseMark = 
    const makeMove = () => {
        
    }
    return {makeMove};
}
